<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Chào mừng đến với API</title>
    <link rel="icon" href="/logo.svg" type="image/x-icon">
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .card {
            border-radius: 20px;
            box-shadow: 0 15px 30px rgba(0,0,0,0.3);
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border: none;
            overflow: hidden;
            transition: all 0.3s ease;
        }
        .card:hover {
            transform: translateY(-10px);
        }
        .btn {
            border-radius: 30px;
            padding: 12px 25px;
            margin: 15px;
            transition: all 0.4s ease;
            font-weight: 600;
            letter-spacing: 0.5px;
        }
        .btn:hover {
            transform: translateY(-5px);
            box-shadow: 0 7px 20px rgba(0,0,0,0.3);
        }
        .display-4 {
            font-weight: 700;
            color: #4a4a4a;
        }
        .lead {
            color: #6c757d;
        }
        .icon-rocket {
            animation: rocket 2s infinite alternate;
        }
        @keyframes rocket {
            0% { transform: translateY(0); }
            100% { transform: translateY(-10px); }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card p-5">
                    <h1 class="display-4 text-center mb-4">
                        <i class="fas fa-rocket icon-rocket me-3 text-primary"></i>Chào mừng đến với API
                    </h1>
                    <p class="lead text-center mb-5">API của bạn đang hoạt động mượt mà và sẵn sàng phục vụ!</p>
                    <div class="d-flex justify-content-center flex-wrap">
                        <a href="/api-swagger" class="btn btn-success btn-lg m-2">
                            <i class="fas fa-book me-2"></i>Xem tài liệu API
                        </a>
                        <a href="/api/views/login" class="btn btn-primary btn-lg m-2">
                            <i class="fas fa-sign-in-alt me-2"></i>Đăng nhập
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
